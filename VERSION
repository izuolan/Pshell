5.1

改动：
- 修改文档，增加 IP 协议端口映射的说明。
- 修改监控函数的延时为0.1秒。
- 修改 Dockerfile，支持自动构建。

5.0

改动：
- 调整帮助输出内容。
- 调整监视器输出信息。
- 优化代码结构。
- 允许自定义代理列表规则。
新增：
- 增加端口流量统计，方便查看代理端口使用了多少流量。
- 新增 -n [set/unset] 选项用于设置流量统计是否开启。

4.0

改动：
- 删除 -c (connect) 选项，全面使用自动重连。
- 重新构建镜像，整合 ICMP/IP 双隧道。
- 默认不再输出容器 CPU 状态，要查看容器状态请使用 -m 选项。
- 更新文档。
新增：
- 添加 IP 协议隧道，使得速度基本没有了限制（实验性功能，安全性有待考究）。
- 使用 -f(--fast) 选项即可启用 IP 隧道。
- 无缝切换隧道协议。

3.0

改动：
- 优化显示输出。
- 优化连接重启策略。

2.0

改动：
- 默认使用自动重连模式，因此去掉 -a (auto) 选项。
- 修复 CentOS/Fedora 下的连接超时判断问题。
- HTTP 代理自动修复。
- 优化代码。
新增：
- 增加不自动重连的 -c (connect) 选项（Debug 时可用）。
- 增加 -u (update) 更新选项。

1.0

为了提供脚本更新提示，新建这个文件用于检测版本。
- 支持服务器自动部署并启动。
- 服务端遇到意外可以自动重启。
- 支持本地自动部署并启动。
- 支持断线自动重连（选择性修复连接）。
- 提供直观的监视器，可以实时查看连接状态。
- 支持指定网卡分享 socks5 代理给他人。
- 支持指定 socks5 端口转发为 http 端口（用于轮询负载）。
- 自动启用 TCP-BBR 算法，极大提高网速（如果内核支持）。
- 密码认证。